#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

        int x, n, m; cin >> x >> n >> m;
        if(x + m < n)cout << "NO" << endl;
        else cout << "YES" << endl;
    
    return 0;
}